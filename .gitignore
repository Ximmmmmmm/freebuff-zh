# Build artifacts — publish these via GitHub Releases instead of committing them
output/
backup/
work/
downloads/

# Local secrets — never commit API keys
.translator.json

# Node / misc
node_modules/
*.log
.DS_Store
Thumbs.db
dist/

# editor / os cruft
.vs/
*.user
