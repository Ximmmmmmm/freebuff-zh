{
  "targetVersion": "0.0.110",
  "packVersion": "0.0.110.1",
  "comment": "targetVersion = 词典适配的 Freebuff Desktop 版本（多开控制器读取做兼容检查）；packVersion = 汉化包自身的发布版本，构建时打进产物（控制器比对是否需要更新）。跟随 targetVersion，同一 targetVersion 内的修正重发追加第四段（如 0.0.103.1）：控制器用四段比较（ParseLooseVersion），已装旧包的机器才会自动拉到修复包；不升版本的同号重发只能 --force，且已装机不会自动更新。"
}
